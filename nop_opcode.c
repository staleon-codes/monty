#include "monty.h"

/**
 * op_nop - this does not do anything
 * @stack: pointer to the top of the stack
 * @line_number: line number of the opcode
 **/
void op_nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
